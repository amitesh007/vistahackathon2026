apiVersion: v1
kind: Service
metadata:
  name: {{ include "loan-service-ui.fullname" . }}
  labels:
    {{- include "loan-service-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "loan-service-ui.selectorLabels" . | nindent 4 }}
