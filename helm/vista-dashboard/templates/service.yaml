apiVersion: v1
kind: Service
metadata:
  name: {{ include "vista-dashboard.fullname" . }}
  labels:
    {{- include "vista-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vista-dashboard.selectorLabels" . | nindent 4 }}
